# ❓ Interactive JavaScript Quiz Game

A classic, fully functional quiz application built with fundamental web technologies. This project is a strong demonstration of **JavaScript control flow**, managing application state, and dynamic DOM manipulation to deliver an engaging user experience.

## ✨ Key Features
* **Dynamic Content:** Questions and answers are loaded from a JavaScript array, allowing for easy expansion of the quiz content.
* **Real-Time Scoring:** Tracks and updates the user's score automatically.
* **Visual Feedback:** Provides immediate visual styling (correct/incorrect) upon answer selection.
* **Game Flow:** Manages the transition between questions and displays the final score with a "Play Again" option.
* **Responsive Design:** Styled to be clean and usable across various screen sizes.

## 💻 Technologies Used
* **HTML5:** Provides the structure for the quiz container, questions, and interactive buttons.
* **CSS3:** Custom styling for a modern, focused user interface, including state-based styling for correct/incorrect buttons.
* **JavaScript (Vanilla):** Implements all core quiz logic, including question traversal, answer checking, scoring, and UI updates.

## 🚀 Getting Started

1.  **Clone the repository:**
    ```bash
    git clone [https://github.com/YourUsername/JS-Interactive-Quiz-App.git](https://github.com/YourUsername/JS-Interactive-Quiz-App.git)
    ```
2.  **Launch:** Simply open the `index.html` file in your web browser. No server setup is required.

## 💡 Customization
The quiz content is stored in the `questions` array within `main.js`. You can easily update this array to create a quiz on any topic!
